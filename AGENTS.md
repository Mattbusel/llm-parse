# llm-parse

Single-header C++17 library for offline HTML/markdown parsing, chunking, and text analysis.

## Structure
- `include/llm_parse.hpp` — single-header implementation
- `examples/` — usage examples
- `CMakeLists.txt` — cmake build (requires vcpkg curl where applicable)

## Build
```bash
cmake -B build && cmake --build build
```
